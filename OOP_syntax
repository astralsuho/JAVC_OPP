2018-12-17

컴퓨터 과학에서 객체 또는 오브젝트(object)는
 1.저장 공간에서 할당되어 값을 가진다&&
 2. 식별자(아이디)에 의해 참조되는 공간. (참조는 주소)
		요소는 index로 참조됨.

 변수,자료구조,함수 또는 메소드가 격체가 될 수 있다.

변수를 이용해 객체에 접근

메모리가 할당되기 전까지 객체는 존재하지 않는다.
Scanner(누워있는애/디스크/클래스) s(아바타/메모리) = new scanner(주소)();

클래스 객체는 자료와 그 자료를 다루는 명령의 조합을 포함하여 객체가 메시지를 받고 자료를 처리하며 
메시지를 다른 객체로 보낼 수 있도록 한다. 실세계의 유추로 설명하자면, 만약 어떤 사람이 집에서 살기를 원할 때, 
그 집의 청사진(집의 사진)이나 축소 모형 따위는 전혀 필요가 없다. 
필요한 것은 설계에 맞는 실제 집이다. 이 유추에서 청사진은 클래스를 나타내고 실제 집은 객체를 나타낸다


s.
클래스종류는 
 class=fields, methods(area), constructors
 
 static = real wolrd
 
 객체지향 프로그래밍 : 상속,은닉화,추상화,다형성
OOP
객체지향프로그밍은 상추다.